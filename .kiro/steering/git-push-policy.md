# Git Push Policy

After completing any code changes, always commit and push to the `master` branch.

## Rules
- Stage only the files that were modified as part of the task
- Write a concise, descriptive commit message summarizing what changed
- Push to `master` using `git push origin master`
- Do this automatically — do not wait for the user to ask
